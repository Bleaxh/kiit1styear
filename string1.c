#include<stdio.h>
int main()
{
    char i=0,src[100],dst[100];
    printf("Enter the string");
    scanf("%s",src);
    while(dst[i]=src[i++]);
    printf("distination string : %s",dst);
    return 0;
}