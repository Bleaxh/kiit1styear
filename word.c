#include<stdio.h>
#include<string.h>
int main(void)
{
    char str1[100],str2[100],i=0;
    printf("Enter the string1 :");
    scanf("%s",arr1);
    printf("Enter the string2 :" );
    scanf("%s",arr2);
    for(i=0,arr1[i]!='\0',i++);
    for(j=0,arr2[j]!='\0',j++)
    {
        if (arr1[i]=arr2[j])
        print("Words found:  ");
        else
        print("\nWords not found :");
    }
    
}    